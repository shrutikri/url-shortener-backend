services:
  - type: web
    name: url-shortener-backend
    runtime: java
    buildCommand: ./gradlew build -x test
    startCommand: java -jar build/libs/url-shortener-backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 21